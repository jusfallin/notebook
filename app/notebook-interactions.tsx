'use client';

// Kept as a compatibility component for older deployments.
// Reader editing is now handled directly by the React button in page.tsx,
// so this component intentionally does not intercept clicks.
export default function NotebookInteractions() {
  return null;
}
