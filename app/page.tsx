'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { AnimatePresence, motion } from 'framer-motion';
import { BookOpen, ChevronLeft, ChevronRight, ImagePlus, Music2, Volume2, VolumeX, Heart, Sparkles, CalendarDays, Wand2, Trash2, Camera, Plus } from 'lucide-react';

type Entry = { id: string; date: string; mood: string; weather: string; text: string; image: string; audio: string; };
type NotebookState = { entries: Entry[]; dedication: string; dedicationPhoto?: string; coverOpened: boolean; };

const STORAGE = 'deka-notebook-v2';
const initialEntries: Entry[] = [{ id: 'first', date: '25.07.2026', mood: '💛', weather: '☀️', text: 'The day our story quietly began.\n\nWrite everything you remember here… the place, the first words, the tiny details you never want to forget.', image: '', audio: '' }];
const moods = ['💛', '❤️', '🥹', '😂', '🫶', '🌙', '✨', '🌸'];
const weather = ['☀️', '🌤️', '🌧️', '⛅', '🌙', '☁️', '🌈'];
function todayString() { const d = new Date(); return `${String(d.getDate()).padStart(2, '0')}.${String(d.getMonth() + 1).padStart(2, '0')}.${d.getFullYear()}`; }
function uid() { return `${Date.now()}-${Math.random().toString(36).slice(2)}`; }

export default function Page() {
  const [state, setState] = useState<NotebookState>({ entries: initialEntries, dedication: 'For the two of us — and every ordinary day that became a memory.', coverOpened: false });
  const [page, setPage] = useState(0); const [edit, setEdit] = useState(false); const [sound, setSound] = useState(true); const [savePulse, setSavePulse] = useState(false); const [heartBurst, setHeartBurst] = useState(0); const [showIndex, setShowIndex] = useState(false); const [openingCover, setOpeningCover] = useState(false); const touchStartX = useRef<number | null>(null);
  useEffect(() => { const raw = window.localStorage.getItem(STORAGE); if (raw) { try { setState(JSON.parse(raw)); } catch {} } }, []);
  useEffect(() => { window.localStorage.setItem(STORAGE, JSON.stringify(state)); setSavePulse(true); const id = window.setTimeout(() => setSavePulse(false), 900); return () => window.clearTimeout(id); }, [state]);
  useEffect(() => { const onKey = (e: KeyboardEvent) => { if (e.target instanceof HTMLInputElement || e.target instanceof HTMLTextAreaElement) return; if (e.key === 'ArrowRight' || e.key === ' ') { e.preventDefault(); next(); } if (e.key === 'ArrowLeft') { e.preventDefault(); prev(); } if (e.key.toLowerCase() === 'e') setEdit(v => !v); if (e.key.toLowerCase() === 'h') makeHearts(); }; window.addEventListener('keydown', onKey); return () => window.removeEventListener('keydown', onKey); });
  const pageCount = 2 + state.entries.length, entryIndex = page >= 2 ? page - 2 : -1, activeEntry = entryIndex >= 0 ? state.entries[entryIndex] : null, isBirthday = activeEntry?.date === '09.09.2026';
  function next() { setPage(p => Math.min(p + 1, pageCount - 1)); } function prev() { setPage(p => Math.max(p - 1, 0)); }
  function updateActive(patch: Partial<Entry>) { if (!activeEntry) return; setState(s => ({ ...s, entries: s.entries.map(e => e.id === activeEntry.id ? { ...e, ...patch } : e) })); }
  function addEntry() { const nextEntry: Entry = { id: uid(), date: todayString(), mood: '✨', weather: '☀️', text: '', image: '', audio: '' }; setState(s => ({ ...s, entries: [...s.entries, nextEntry] })); setPage(2 + state.entries.length); setEdit(true); }
  function duplicateEntry() { if (!activeEntry) return; const clone = { ...activeEntry, id: uid(), date: todayString() }; setState(s => ({ ...s, entries: [...s.entries, clone] })); setPage(2 + state.entries.length); }
  function deleteEntry(id: string) { if (state.entries.length <= 1) { window.alert('Keep at least one memory page in your diary.'); return; } const index = state.entries.findIndex(e => e.id === id); if (index < 0) return; if (!window.confirm(`Remove the page dated ${state.entries[index].date}? This cannot be undone.`)) return; setState(s => ({ ...s, entries: s.entries.filter(e => e.id !== id) })); setPage(p => { const newCount = 2 + state.entries.length - 1; return Math.min(Math.max(p > index + 2 ? p - 1 : p, 0), newCount - 1); }); }
  function handleFile(type: 'image' | 'audio', file?: File) { if (!file || !edit) return; const reader = new FileReader(); reader.onload = () => updateActive({ [type]: String(reader.result) }); reader.readAsDataURL(file); }
  function makeHearts() { setHeartBurst(v => v + 1); window.setTimeout(() => setHeartBurst(v => v + 1), 700); }
  function removeEntryMedia(type: 'image' | 'audio') { updateActive({ [type]: '' }); }
  function openCover() { if (openingCover) return; setOpeningCover(true); setState(s => ({ ...s, coverOpened: true })); window.setTimeout(() => { setPage(1); setOpeningCover(false); }, 620); }
  const title = useMemo(() => page === 0 ? 'DEKA NOTEBOOK' : page === 1 ? 'A LITTLE PLACE FOR US' : activeEntry?.date ?? '', [page, activeEntry]);
  return <main className="scene" aria-label="Deka Notebook" onTouchStart={e => { touchStartX.current = e.changedTouches[0].clientX; }} onTouchEnd={e => { if (touchStartX.current === null) return; const dx = e.changedTouches[0].clientX - touchStartX.current; if (Math.abs(dx) > 50) (dx < 0 ? next : prev)(); touchStartX.current = null; }}>
    <div className="ambient" /><div className="heart-layer" aria-hidden="true">{heartBurst > 0 && Array.from({ length: 14 }, (_, i) => <motion.span key={`${heartBurst}-${i}`} initial={{ x: '50%', y: '58%', scale: 0, opacity: 0 }} animate={{ x: `${12 + (i * 67) % 76}%`, y: `${18 + (i * 31) % 68}%`, scale: [0, 1.1, .8], opacity: [0, 1, 0] }} transition={{ duration: 1.8, delay: (i % 5) * .05 }} className="floating-heart">{i % 3 === 0 ? '❤️' : '♥'}</motion.span>)}</div>
    <header className="toolbar"><div className="brand"><Heart size={16} fill="currentColor" /> DEKA</div><div className="toolbar-center"><span className="title-dot" /> {title} <span className="title-dot" /></div><div className="toolbar-actions"><button onClick={makeHearts} aria-label="send love"><Heart size={17} fill="currentColor" /></button><button onClick={() => setShowIndex(v => !v)} aria-label="open notebook index"><BookOpen size={17} /></button><button onClick={() => setSound(v => !v)} aria-label="toggle rustle sound">{sound ? <Volume2 size={18}/> : <VolumeX size={18}/>}</button><button onClick={() => setEdit(v => !v)}>{edit ? 'Done' : 'Edit'}</button><span className="saved">{savePulse ? 'Saved ♥' : 'Auto-saved'}</span></div></header>
    <AnimatePresence>{showIndex && <motion.aside className="index-panel" initial={{ x: 380, opacity: 0 }} animate={{ x: 0, opacity: 1 }} exit={{ x: 380, opacity: 0 }}><div className="index-head"><span>OUR STORY</span><button onClick={() => setShowIndex(false)}>×</button></div><button className={`index-item ${page === 0 ? 'active' : ''}`} onClick={() => {setPage(0);setShowIndex(false)}}><span>Cover</span><small>DEKA NOTEBOOK</small></button><button className={`index-item ${page === 1 ? 'active' : ''}`} onClick={() => {setPage(1);setShowIndex(false)}}><span>Dedication</span><small>A little place for us</small></button>{state.entries.map((e,i)=><div className="index-row" key={e.id}><button className={`index-item ${page === i+2 ? 'active':''}`} onClick={()=>{setPage(i+2);setShowIndex(false)}}><span>{e.date} <b>{e.mood}</b></span><small>{i===0?'Where it all began':'A page from our life'}</small></button><button className="index-delete" type="button" aria-label={`remove page ${e.date}`} title="Remove this page" onClick={()=>deleteEntry(e.id)} disabled={state.entries.length<=1}><Trash2 size={14}/></button></div>)}</motion.aside>}</AnimatePresence>
    <section className="desk" aria-label="digital notebook"><AnimatePresence mode="wait">{page === 0 ? <motion.button key="cover" className={`cover ${state.coverOpened || openingCover?'opened':''}`} initial={{rotateY:-10,y:20,scale:.97}} animate={{rotateY:openingCover?-72:0,y:0,scale:openingCover?.985:1}} exit={{rotateY:90,x:80,opacity:0,transition:{duration:.72}}} transition={{duration:openingCover?.62:.45,ease:[.22,1,.36,1]}} onClick={openCover} disabled={openingCover} aria-label="Open the DEKA diary">
      <span className="cover-rim"/><span className="cover-stitch"/><span className="cover-corner top-left"/><span className="cover-corner top-right"/><span className="cover-corner bottom-left"/><span className="cover-corner bottom-right"/><span className="cover-band"/>
      <div className="cover-small">PRIVATE JOURNAL</div><div className="cover-monogram">DB</div><div className="cover-title">DEKA</div><div className="cover-rule"><i/><span>♡</span><i/></div><div className="cover-subtitle">OUR DAYS · OUR WORDS · OUR MEMORIES</div><div className="cover-name">only for my girl <span>♥</span></div><div className="cover-footer"><span>25 · 07 · 2026</span><span>VOL. I</span></div><span className="ribbon"/><span className="cover-edge-label">OURS</span><span className="cover-open-hint">tap to open · our story begins here</span>
    </motion.button> : <motion.div key={page} className="book-wrap" initial={{rotateY:page>0?-14:14,x:page>0?30:-30,opacity:.45}} animate={{rotateY:0,x:0,opacity:1}} transition={{duration:.62,ease:[.22,1,.36,1]}}><article className={`paper ${isBirthday?'birthday-paper':''}`}>
      <div className="paper-shadow"/><div className="page-curl"/><div className="margin-line"/>{page===1?<section className="dedication spread"><div className="eyebrow"><span>✦</span> THE DEDICATION <span>✦</span></div><h1>A little place for us.</h1><p className="handwritten">For the two of us —<br/>for every laugh, every tiny fight,<br/>every “remember when…”,<br/>and every ordinary day worth keeping.</p><label className="photo-frame">{state.dedicationPhoto?<><img src={state.dedicationPhoto} alt="our favorite photograph"/>{edit&&<button type="button" className="remove-photo" aria-label="remove dedication photo" onClick={e=>{e.preventDefault();setState(s=>({...s,dedicationPhoto:''}))}}><Trash2 size={15}/></button>}</>:<><Camera size={25}/><span>Place our favorite photograph here</span></>}<input type="file" accept="image/*" hidden onChange={e=>{const f=e.target.files?.[0];if(!f)return;const r=new FileReader();r.onload=()=>setState(s=>({...s,dedicationPhoto:String(r.result)}));r.readAsDataURL(f)}}/></label>{!state.dedicationPhoto&&edit&&<textarea className="dedication-note" value={state.dedication} onChange={e=>setState(s=>({...s,dedication:e.target.value}))}/>}<div className="flourish">❦</div><button className="text-link" onClick={()=>setPage(2)}>Begin the story →</button></section>:activeEntry?<section className="entry spread">{isBirthday&&<motion.div className="birthday-banner" initial={{y:-15,opacity:0}} animate={{y:0,opacity:1}}><Sparkles size={15}/> 09.09.2026 · A SPECIAL PAGE FOR YOU <Sparkles size={15}/></motion.div>}<div className="entry-topline"><div>{edit?<input className="date-input" value={activeEntry.date} onChange={e=>updateActive({date:e.target.value})}/>:<div className="date-display">{activeEntry.date}</div>}<div className="entry-label">{isBirthday?'THE DAY WE CELEBRATE YOU':'A PAGE FROM OUR LIFE'}</div></div><div className="stamps"><button className="stamp" title="change mood" onClick={()=>{const idx=moods.indexOf(activeEntry.mood);updateActive({mood:moods[(idx+1)%moods.length]})}}>{activeEntry.mood}</button><button className="stamp" title="change weather" onClick={()=>{const idx=weather.indexOf(activeEntry.weather);updateActive({weather:weather[(idx+1)%weather.length]})}}>{activeEntry.weather}</button></div></div><div className="rule"/>{isBirthday&&<p className="birthday-line">Happy birthday, my girl. From this page onward, we write our story together. ♡</p>}{edit?<textarea className="entry-editor" value={activeEntry.text} placeholder="Write today's memory…" onChange={e=>updateActive({text:e.target.value})}/>:<div className="entry-text">{activeEntry.text}</div>}<div className="media-grid"><label className="media-card media-photo">{activeEntry.image?<><img src={activeEntry.image} alt="memory"/>{edit&&<button type="button" className="remove-media" aria-label="remove photo" onClick={e=>{e.preventDefault();removeEntryMedia('image')}}><Trash2 size={14}/></button>}</>:<><ImagePlus size={22}/><span>Photo</span></>}<input type="file" accept="image/*" hidden disabled={!edit} onChange={e=>handleFile('image',e.target.files?.[0])}/></label><label className="media-card audio-card">{activeEntry.audio?<><audio controls src={activeEntry.audio}/>{edit&&<button type="button" className="remove-media" aria-label="remove audio" onClick={e=>{e.preventDefault();removeEntryMedia('audio')}}><Trash2 size={14}/></button>}</>:<><Music2 size={22}/><span>Song / voice note</span></>}<input type="file" accept="audio/*" hidden disabled={!edit} onChange={e=>handleFile('audio',e.target.files?.[0])}/></label></div><div className="page-footer"><span>{entryIndex+1} / {state.entries.length}</span><span className="footer-heart">♡</span>{edit&&<><button className="duplicate" onClick={duplicateEntry}>Duplicate page</button><button className="delete-page" onClick={()=>activeEntry&&deleteEntry(activeEntry.id)} disabled={state.entries.length<=1} title={state.entries.length<=1?'Keep at least one memory page':'Remove this page'}><Trash2 size={14}/> Remove page</button></>}</div></section>:null}</article></motion.div>}</AnimatePresence><button className="nav prev" onClick={prev} disabled={page===0} aria-label="previous page"><ChevronLeft size={24}/></button><button className="nav next" onClick={next} disabled={page===pageCount-1} aria-label="next page"><ChevronRight size={24}/></button></section>
    <footer className="bottom-bar"><div className="progress"><span>{page+1}</span><i/><span>{pageCount}</span></div>{page>=1&&<button className="new-entry" onClick={addEntry}><Plus size={18}/> New page</button>}<button className="love-button" onClick={makeHearts}><Wand2 size={15}/> sprinkle love</button><div className="hint"><CalendarDays size={15}/> 25.07.2026 → forever</div></footer>
  </main>;
}
